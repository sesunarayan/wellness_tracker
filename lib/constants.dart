import 'package:flutter/material.dart';

const double kPadding = 12.0;
const double kCardRadius = 16.0;

final kShadow = [
  BoxShadow(
    color: Colors.black.withAlpha(1 * 225),
    blurRadius: 6,
    offset: const Offset(0, 3),
  )
];
